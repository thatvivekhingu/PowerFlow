{
  "name": "gridmind-blockchain",
  "version": "1.0.0",
  "description": "GRIDMIND Smart Contract & Local EVM Blockchain",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.18"
  }
}

