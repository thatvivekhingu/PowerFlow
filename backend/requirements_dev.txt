# ── GRIDMIND local dev dependencies (no PostgreSQL or Redis server needed) ────
# Swap: asyncpg → aiosqlite, redis-server → fakeredis
# Install: pip install -r requirements_dev.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database - SQLite (no server, file-based)
sqlalchemy>=2.0.35
aiosqlite>=0.20.0

# Redis replacement - pure Python in-memory
fakeredis>=2.26.0

# Auth + config
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1

# Simulation
numpy>=1.26.0
pandas>=2.2.0

# HTTP client
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

